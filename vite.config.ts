import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // Set the correct base for GitHub Pages deployment
  // Replace with the repository name as the path base
  base: '/game.newjogodobicho.prototype/',
})
